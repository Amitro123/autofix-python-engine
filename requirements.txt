# Minimal dependencies for AutoFix Python error fixer
# No AI dependencies, no multi-language support, no VSCode extension

# Core Python standard library modules are used
# No external dependencies required for basic functionality